common:
  save_config: false

sim_logger:
  animation_enabled: false

map:
  # path from pkg share dir
  yaml_path: "env/final_ver3/occupancy_grid_map.yaml"

waypoints:
  # path from pkg share dir
  # csv_path: "maps/occupancy_grid_map_waypoints.csv"
  csv_path: ""

obstacles:
  # path from pkg share dir
  csv_path: "" # If empty, the obstacles information will be subscribed from the topic
  # csv_path: "maps/occupancy_grid_map_obstacles.csv"
  radius: 1.25

reference_path:
  update_by_topic: false # When true, 'circle' must be false
  # path from pkg share dir
  # csv_path: ""
  csv_path: "env/final_ver3/traj_mincurv_manual.csv"
  resolution: 0.6
  smoothing_distance: 2
  max_width: 6.0
  circular: true
  use_path_constraints_topic: false
  use_border_cells_topic: false
  # use_path_constraints_topic: true
  # use_border_cells_topic: true

bicycle_model:
  length: 1.087
  # width: 1.45   # real machine width
  width: 2.30   # +safety margin

mpc:
  N: 20
  # Q: [e_y, e_psi, t]
  # R: [v, delta]
  # QN: [e_y, e_psi, t]
  R: [100000.0, 0.0]
  a_min: -0.9 # m/s^2
  a_max: 3.0 # m/s^2
  delta_max_deg: 32.0 # degrees
  steer_rate_max: 0.35 # rad/s
  control_rate: 40.0 # Hz
  steering_tire_angle_gain_var: 1.639
  # steering_tire_angle_gain_var: 1.0
  # accel_low_pass_gain: 0.1
  # steer_low_pass_gain: 0.3
  accel_low_pass_gain: 1.0
  steer_low_pass_gain: 1.0
  wp_id_offset: 2
  use_max_kappa_pred: true
  lateral_target_mode: "reference_path"
  wall_margin_m: 0.25
  use_curvature_speed_profile: true
  use_ref_vel_as_speed_cap: true
  speed_profile_debug_publish_period_sec: 0.25
  neutral_horizon_publish_period_sec: 0.05
  use_grade_accel_feedforward: true
  grade_ff_gain: 1.0
  grade_ff_max_accel_mps2: 0.35
  grade_ff_window_m: 2.0
  grade_ff_min_distance_delta_m: 0.5
  grade_ff_min_speed_mps: 0.3
  grade_ff_low_pass_gain: 0.25
  grade_ff_only_when_not_decelerating: true
  grade_ff_speed_error_deadband_mps: 0.1


  ## ---------- 究極 ------------- ##
  ## Lap1: 53s, Lap2: 47s

  v_max: 35.0   # km/h
  # dev3 20260719-035155ではs5へ7.4 m/sで進入し、D2が壁際で187秒停止した。
  # 既存の安定走行プロファイルへ戻し、曲率と前後加速度制約で入口から減速する。
  ay_max: 9.5  # m/s^2, maximum lateral acceleration in curves
  Q: [10000000.0, 100000000.0, 100000.0]
  QN: [1000000.0, 1000000.0, 10000.0]

  ## --- 高速 (gnss delay 0.3) --- ##
  ## Lap1: 55s, Lap2: 49s

  # v_max: 30.0
  # ay_max: 9.5
  # Q: [1000000.0, 100000000.0, 850000.0]
  # QN: [1000000.0, 1000.0, 10000.0]

  ## --- 高速 (gnss delay 0.4) --- ##
  ## Lap1: 55s, Lap2: 49s

  # v_max: 30.0
  # ay_max: 9.5
  # Q: [1000000.0, 100000000.0, 1850000.0]
  # QN: [1000000.0, 1000.0, 10000.0]

  ## --- 中高速 --- ##
  ## Lap1: 59s, Lap2: 54s
  # v_max: 25.0
  # ay_max: 7.0
  # Q: [1000000.0, 100000000.0, 850000.0]
  # QN: [1000000.0, 1000.0, 10000.0]

  ## --------- 中速 -------------- ##
  ## Lap1: 67s, Lap2: 64s

  # v_max: 20.0
  # ay_max: 6.5
  # Q: [3000000.0, 90000000.0, 100000.0] # 低速だとインを攻めがちなので、あえて外に膨らむパラメータ
  # QN: [1000000.0, 1000.0, 10000.0]

  ## -------- 低速 -------------- ##
  ## Lap1: 86s

  # v_max: 15.0
  # ay_max: 5.5
  # Q: [5000000.0, 100000000.0, 200000.0] # 低速だとインを攻めがちなので、あえて外に膨らむパラメータ
  # QN: [1000000.0, 1000.0, 10000.0]

v2x_obstacle_avoidance:
  vehicle_radius: 0.5          # [m]
  prediction_obstacle_min_spacing_m: 0.50 # [m]
  v_max_safety: 30.0           # [m/s]
  position_jump_threshold: 5.0 # [m]
