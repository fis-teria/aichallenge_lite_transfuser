# 未充足のコーナー復帰条件の追加収集

前回の封印済み収集 `time_corner_gap_20260916` に残った28条件を対象に、
別ルート `time_corner_gap2_20260916` で12走行、1周最大3イベントの追加収集を実施した。
実行先は graneple@192.168.3.10、ROS_DOMAIN_ID 1/2 とネットワーク名前空間を分離した2台。
AWSIM本体を変更せず、目標速度5km/h、通常RVizの経路・外乱マーカー表示を維持する。
取得上限速度超過は既存の `record_actual_v1` に従い記録する。

## 準備経路の補正

C02/C05/C07は前回の実測オーバーシュートに合わせ、準備経路だけを内側へ補正する。
C06の20cm条件は、前回の同一地点付近で nominal_path が約8cm、
measured_normal が約28cmの横ずれを生んだため、中間の準備経路を選択可能にする。

`LargeRecoverySite.preparation_origin="blended_normal"` は、元の指令経路と
通常走行の実測軌跡のx/yを `preparation_normal_fraction` (無次元、0..1、既定0.5)
で補間する。0はnominal_path、1はmeasured_normalと同じ準備位置になる。
既存2モードの幾何形状、測定用の通常走行座標系、目標の位置・横ずれ・向きは変更しない。
生成した経路には従来の車体占有チェックと実走時の停止領域監視を適用する。
この補間は収集の準備にのみ使用し、教師には通常PPへ切替後の実測3秒軌道を使う。

C03の60cmは、長い横ずれ生成経路が地図占有チェックを通らず、短い経路では
切替地点で連続1秒の開始条件が揃わなかった。そこで `preparation_delay_m`
(0..6m、既定0m) を追加し、準備コントローラへの切替後も前半は元の経路を進み、
後半の6m以上で横ずれを生成できるようにする。開始時の空間余裕・安定時間の条件は
維持し、地図チェック対象の区間や準備時間の制限も元の切替位置から計算する。
既定0mでは旧経路と同一。目標地点の移動や判定基準の緩和は行わない。

## 実行と検証

Windowsで変更をコミットし、`python tmp/time_ten_site_recovery_plan_20260915/sync_native_transport.py sync`
で同じコミットをWSLへ同期する。WSLのネイティブ作業ツリーで、
`tools/with_wsl_training_lock.sh .venv/bin/python -m pytest -q`
を実施する。同期・教師検証を同時に走らせない。

収集の操作スクリプトは `tmp/time_corner_gap2_20260916`。
`ops_corner.py setup`、`run_native_script.py adaptive_plan_v3.py ...`、
`start_monitor.py --pair N`、`collect_pair.py --pair N --left PLAN --right PLAN`、
`audit_pair.py --pair N` の順で、走行・転送照合・教師作成を行う。
2走行ごとにファイルハッシュ・ディレクトリ構成・SQLiteを検証してWSLへ移し、
確認済みの当該リモート生データのみ削除する。旧収集は保全する。

充足判定は従来どおり、完走した同一イベント内に有効教師60点以上と、
元の横ずれ±5cm・向き±1度の条件を満たす開始教師3点以上を要求する。
run単位でdomain 1をtrain、domain 2をvalidationへ割り当てる。

## 実測結果

12走行のうち8走行が完走、4走行は停止領域監視による停止。
完走した走行から19イベント、2,429サンプルを採用した。
1サンプルは因果的なCamera/LiDAR/ego入力と実測の将来3秒・30点のxy軌道であり、
隣接サンプルの時間窓は重なる。新しい学習やE2Eモデルの性能評価はまだ行っていない。

| 目標横ずれ | train | validation | 合計 | 採用イベント |
| --- | ---: | ---: | ---: | ---: |
| 20cm | 389 | 249 | 638 | 5 |
| 40cm | 385 | 358 | 743 | 6 |
| 60cm | 920 | 128 | 1,048 | 8 |
| 合計 | 1,694 | 735 | 2,429 | 19 |

生データ12,721,128,710 bytes（約12.72 GB）をWSLへ転送し、全ファイルのハッシュ、
ディレクトリ構成、SQLite quick_checkを照合した。教師は `[N,30,2]`、有限値、
全future mask有効、全採用入力有効。run間のtrain/validation重複はない。
旧取得上限1.4m/sを超えた146サンプルも採用し、速度上限超過による収集中止は0件。
全走行の最大実測速度は5.1804km/h。採用イベント内の制御ログのsimulation timestamp間隔は
最大85msであり、これは推論レイテンシの測定ではない。

| 組 | train側 | validation側 | 採用サンプル |
| --- | --- | --- | ---: |
| 01 | C02/C05の40cm、C07の20cmを完了・完走 | C02/C05/C07の40cmを完了・完走 | 736 |
| 02 | C02の60cm準備中に停止監視 | C02の60cm切替中に停止監視 | 0 |
| 03 | C04の60cm、C06の20cmを完了・完走。C09は目標姿勢未達 | C04の60cm、C06の20cmを完了・完走 | 502 |
| 04 | C02/C05/C08の60cmを完了・完走 | C02の60cm復帰開始直後に停止監視 | 409 |
| 05 | C07の40cm、C10の60cmを完了・完走。C03は目標姿勢未達 | C01の60cm準備中に停止監視 | 283 |
| 06 | C04/C09の60cm、C06の20cmを完了・完走 | C06の20cmを完了・完走。C03は継続・位置期限条件を満たさず | 499 |

各組で2つのAWSIMインスタンスを使った。同時に2台が進行していた実測時間は、
順に226.14/0/226.14/60.22/20.38/224.95秒。組02は最初の車が早期停止したため
同時走行区間がない。ホストの最小MemAvailableは5.70GiBだった。

## 残る条件の区別

元の条件で未充足だった28条件は16条件に減った。
ここでの条件は「横ずれ量×コーナー×train/validation」の組合せ。
16条件のうち7条件には既に採用済みの復帰教師があり、同一イベント内の開始教師3点という
追加条件を満たしていない。残り9条件は元の条件に対応する採用済み復帰教師がない。
「未充足16条件」を「教師データが全くない16条件」と解釈しない。

| 横ずれ | train未充足 | validation未充足 |
| --- | --- | --- |
| 20cm | C06 | C06 |
| 40cm | C06 | C05、C06 |
| 60cm | C01、C03、C05、C06、C07 | C01、C02、C03、C05、C06、C07 |

- C06の20cmは今回4イベント・503サンプルを採用できた。厳密な開始状態の教師は
  train側の各イベントが0/2点、validation側が0/1点で、充足判定は未達のまま。
- C05の40cm・validationには既存分込みで3イベント・304サンプルがある。
  開始状態の教師は合計6点あるが、各イベントの最大は2点。
- C03の60cmは遅延した横ずれ生成で準備を開始できた。最初の候補は約60cm・約6度になり、
  4±1度の条件を満たさなかった。補正後は位置134.701mで横ずれ59.27cm・向き4.69度まで
  到達したが、250msの連続成立とrelease+1mまでの切替条件を同時に満たせなかった。
  その後の通常走行は完走したが、当該イベントは教師に採用していない。
- C01の60cmは、今回の遅延した生成経路でも準備中に停止領域監視が作動した。
  C02の60cmはtrainでは採用できたが、validationの今回の2試行は停止監視にかかった。
  この結果だけで、別の経路でも復帰不能とは断定しない。
- C06の40/60cmは、元の目標位置付近で調べた36姿勢/横ずれ量がすべて車体地図チェック不合格。
  これは有限個の静的サンプルの結果であり、全連続状態に対する不可能性の証明ではない。

手前の別条件として、C01の進行位置22m（元26.85m）、C06の204m（元210.28m）を調べた。
各候補で調べた27姿勢と準備経路は地図チェックを通った。ただし別位置の実走は未実施で、
今回の収集数・元条件の充足数には含めていない。元の停止地点から収集位置を変える案として
方針確認を提示している。静的合格を実走での復帰成功とは扱わない。

## 保存先・検証記録

- WSL raw: `/home/thistle/e2e_autonomous/raw/time_corner_gap2_20260916`
- WSL教師・入力cache・index: `/home/thistle/e2e_autonomous/runs/time_corner_gap2_20260916`
- [収集index](evidence/time_corner_gap2_20260916/collection_index.json)
- [充足判定](evidence/time_corner_gap2_20260916/coverage_final.json)
- [残条件ごとの既存教師数](evidence/time_corner_gap2_20260916/remaining_coverage_diagnosis.json)
- [AWSIM・ホスト終了確認](evidence/time_corner_gap2_20260916/host_final_checks.json)
- [WSL最終照合PASS](evidence/time_corner_gap2_20260916/final_verification.json)

最終照合はWindows/WSLを証拠コミット`3c485a8`に揃え、WSL worktree lock内で実施。
梱包した201ファイル、参照経路のハッシュ、実行ソースのテスト記録、全採用教師を再照合した。
`final_verification.json`はこの照合後の記録であり、元の`manifest.json`には含めない。
元の条件すべての充足については、照合結果も明示的にfalseとしている。

実行コードは組01が`36b47e1`、組02/03が`48ae7af`、組04〜06が`1b845ed`。
最終コードのWSL pytestは2,759 passed / 4 skipped / 84 warnings。
新規処理は補間の端点互換性、実測目標の不変性、shape・単位、障害物拒否、無効な係数、
横ずれ生成の遅延と従来短区間経路の一致を回帰テストで確認した。

最終集計の実行コマンド:

```text
python tmp/time_corner_gap2_20260916/run_native_script.py finalize_native.py
python tmp/time_corner_gap2_20260916/run_native_script.py coverage_diagnosis.py
python tmp/time_corner_gap2_20260916/finish_remote.py
python tmp/time_corner_gap2_20260916/pack_evidence.py
```

AWSIMの1,089ファイルと元の実行リポジトリのHEAD/作業状態は変更なし。
全収集コンテナ・監督プロセスは終了し、転送照合後に当該リモート生データだけを削除した。
リモート空き容量は14.64GiB。旧収集データは保全し、新しい収集ルートは封印した。
再実行には新しいルートと有限の走行予算を使用する。
今回分は既存データへ物理統合しておらず、再学習・E2E閉ループ評価も未実施。
