These are historical operators for the named, bounded experiment. Paths, run IDs,
and exclusive outputs refer to the completed run; do not rerun them unchanged.
Canonical user-facing entry points and limits are documented in
../../../mppi_v45_close_entry_adjustment.md.
Raw recordings remain in WSL; only small receipts and diagnostics are committed.
