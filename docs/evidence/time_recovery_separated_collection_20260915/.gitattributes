# Preserve the exact bytes checked by artifact_manifest.json across Git/WSL.
* -text
# Keep the captured script's original CRLF and terminal blank line for its hash.
fetch_marker_proofs.py whitespace=cr-at-eol,-blank-at-eof
