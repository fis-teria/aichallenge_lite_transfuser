format_version: aic_split_config_v1
# Selected against the accepted 15-run inventory so every collection case has
# train/validation/test = 3/1/1 and the two implausible yaw-rate frames remain
# outside the training split.
split_seed: 4904
ratios:
  train: 0.60
  validation: 0.20
  test: 0.20
fixed_benchmark_group_ids: []
unknown_value: unknown
